# PROJECT_SPEC

## 1. 프로젝트 개요

- **서비스명:** 교육 운영 관리 서비스 (가칭)
- **한 줄 소개:** 회사·기관의 교육 담당자가 교육 생성부터 신청자 관리, 연락 현황, 출석 체크, 만족도 조사까지 한곳에서 관리할 수 있는 웹서비스
- **해결하려는 문제:**
  - 새로운 교육이 생길 때마다 기존 구글폼을 복사하고 교육명, 일정 등을 수정해야 한다.
  - 신청자 정보는 별도의 스프레드시트에서 확인해야 한다.
  - 신청자에게 전화 또는 문자를 보낸 뒤 연락 상태를 따로 기억하거나 관리해야 한다.
  - 교육 당일 출석 여부를 별도로 관리해야 한다.
  - 교육 종료 후 만족도 조사용 구글폼을 다시 운영해야 한다.
  - 만족도 미응답자를 확인하려면 신청자 명단과 설문 응답 결과를 따로 비교해야 한다.
  - 교육이 늘어날수록 신청폼, 스프레드시트, 연락 상태, 출석 정보, 만족도폼이 분산되어 관리가 복잡해진다.
- **주요 사용자:** 회사·기관에서 교육 프로그램을 운영하고 수강생을 직접 관리하는 교육 담당자
- **핵심 가치:** 교육 하나를 기준으로 신청, 신청자 정보, 연락 상태, 출석, 만족도 조사와 결과를 한곳에서 이어서 관리하여 반복적인 폼 생성과 분산된 운영 업무를 줄인다.

### 초기 구현 범위 원칙

- 최초 버전은 **정적 웹사이트 프로토타입**으로 구현한다.
- 로그인 화면과 로그인 버튼 UI는 구현한다.
- 최초 버전에서는 실제 로그인 인증을 연동하지 않고, 로그인 버튼 클릭 시 관리자 화면으로 이동하는 형태로 구현한다.
- 실제 로그인/회원가입이 필요해지면 추후 Supabase Auth 연동을 고려한다.
- 데이터베이스는 최초 버전에 연결하지 않는다.
- 실제 데이터 저장이 필요해지면 추후 Supabase Database 연동을 고려한다.
- 최초 버전의 교육, 신청자, 출석, 연락 상태, 만족도 데이터는 Mock Data를 사용한다.
- 실제 주민등록번호나 실제 개인정보는 사용하지 않는다.

---

## 2. 서비스 사용 시나리오

### 2.1 사용자가 서비스를 사용하는 상황

#### 관리자 진입
1. 교육 담당자가 관리자 로그인 화면에 접속한다.
2. 아이디 또는 이메일과 비밀번호를 입력한다.
3. 최초 버전에서는 실제 인증 없이 로그인 버튼을 누르면 `교육 관리` 화면으로 이동한다.
4. 실제 인증은 추후 Supabase 연동 시 구현한다.

#### 신규 교육 생성
1. 담당자가 `교육 관리` 화면에서 `새 교육 생성`을 선택한다.
2. 교육명, 교육일, 시작시간, 종료시간, 장소, 정원을 입력한다.
3. 신청 마감 시각은 교육일 하루 전 17:00으로 자동 계산한다.
4. 교육 생성 시 공통 신청 항목과 생성 당시의 공통 만족도 문항을 적용한다.
5. 생성 후 교육 상세 화면으로 이동한다.

#### 교육 신청
1. 수강생은 교육 신청 페이지에 접속한다.
2. 이름, 전화번호, 주민등록번호 전체, 소속을 입력한다.
3. 개인정보 수집·이용에 동의한다.
4. 신청 마감 이전이면 `정상 신청`으로 처리한다.
5. 신청 마감 이후이면 `마감 후 신청`으로 처리한다.
6. 교육 종료 이후 신청도 `마감 후 신청`으로 처리한다.
7. 동일 교육에서 같은 전화번호가 이미 존재하면 중복 경고를 표시한다.
8. 사용자가 계속 진행하면 중복 신청을 그대로 접수한다.

#### 신청자 관리
1. 담당자는 신청자 목록을 확인한다.
2. 모든 신청자는 하나의 목록에 표시한다.
3. 마감 후 신청자는 `마감 후 신청` 배지로 구분한다.
4. 중복 신청자는 `중복 신청` 문구를 표시한다.
5. 담당자는 이름, 전화번호, 주민등록번호, 소속을 수정할 수 있다.
6. 신청 취소 시 삭제하지 않고 `취소` 상태로 변경한다.
7. 취소 상태는 다시 신청 상태로 복구할 수 있다.
8. 취소된 신청자는 현재 신청 인원과 정원 초과 계산에서 제외한다.

#### 명단 다운로드
1. 담당자는 전체 신청자 또는 선택한 신청자만 `.xls`로 내려받는다.
2. 다운로드 항목은 이름과 전화번호만 사용한다.
3. 전화번호는 하이픈을 제거한다.
4. 파일명은 `YYYYMMDD_교육명_신청자명단.xls` 형식을 사용한다.

#### 연락 관리
1. 통화 상태를 `미통화 / 부재 / 통화완료` 중 하나로 기록한다.
2. 문자 상태를 `미발송 / 발송완료` 중 하나로 기록한다.
3. 통화와 문자는 독립적으로 관리한다.
4. 실제 전화 또는 문자 발송은 구현하지 않는다.

#### 출석 관리
1. 교육 당일 신청자별로 `미확인 / 출석 / 결석`을 선택한다.
2. 취소된 신청자는 출석 대상에서 제외한다.
3. 출석, 결석, 미확인 인원을 확인한다.

#### 만족도 조사
1. 담당자는 만족도 관리 탭에서 Mock QR 이미지를 보여준다.
2. QR 이미지를 클릭하면 `survey.html`로 이동한다.
3. 수강생은 이름을 입력한다.
4. 점수형과 주관식 문항에 응답한다.
5. 한 수강생은 최초 1회만 응답 가능하다.
6. 응답 가능 기간은 교육 종료일 기준 이틀 뒤 23:59까지이다.
7. 이미 응답한 경우 `이미 만족도 조사에 참여하셨습니다.`를 표시한다.
8. 응답 기간 종료 후에는 `만족도 조사 응답 기간이 종료되었습니다.`를 표시한다.

#### 만족도 결과
1. 담당자는 전체 응답자 수를 확인한다.
2. 문항별 평균 점수를 확인한다.
3. 주관식 답변을 확인한다.
4. 특정 응답자를 선택해 사람별 상세 응답을 확인한다.

#### 공통 만족도 문항 관리
1. 점수형 또는 주관식 문항을 추가한다.
2. 문항을 수정하거나 삭제한다.
3. `위로 / 아래로` 버튼으로 순서를 변경한다.
4. 변경된 공통 문항은 이후 새로 생성되는 교육부터 적용한다.
5. 이미 생성된 교육의 문항은 변경하지 않는다.

### 2.2 사용 전과 사용 후의 변화

#### 사용 전
- 교육이 생길 때마다 기존 구글폼을 복사하고 교육명과 일정을 수정한다.
- 신청자 정보는 스프레드시트에서 따로 확인한다.
- 전화·문자 연락 여부를 따로 관리한다.
- 출석 여부를 별도 명단으로 관리한다.
- 교육 종료 후 만족도 구글폼을 따로 운영한다.
- 미응답자를 찾기 위해 신청자 목록과 설문 응답을 비교한다.

#### 사용 후
- 교육 기본 정보만 입력해 교육을 생성한다.
- 신청 정보와 정원 현황을 한곳에서 확인한다.
- 정상 신청과 마감 후 신청을 같은 목록에서 구분한다.
- 통화, 문자, 출석 상태를 신청자별로 관리한다.
- 교육 종료 후 만족도 조사, 응답 현황, 미응답자, 만족도 결과를 같은 교육 화면에서 확인한다.
- 신청자 이름과 하이픈이 제거된 전화번호만 `.xls`로 다운로드한다.

---

## 3. 핵심 기능

### 기능 1. 교육 생성 및 공통 양식 자동 적용
- **사용자 행동:** 담당자가 교육명, 교육일, 시작시간, 종료시간, 장소, 정원을 입력하고 교육을 생성한다.
- **기능 설명:** 교육일을 기준으로 신청 마감 시각을 하루 전 17:00으로 자동 계산한다. 모든 교육에 동일한 신청 항목을 사용하며, 생성 시점의 공통 만족도 문항을 적용한다.
- **필요한 이유:** 교육이 생길 때마다 기존 구글폼을 복사하고 내용을 수정하는 반복 작업을 줄이기 위해 필요하다.

### 기능 2. 교육 신청 및 신청자 명단 관리
- **사용자 행동:** 수강생이 신청 정보를 입력하고 개인정보 수집·이용에 동의한 뒤 신청한다. 담당자는 신청자 정보를 확인, 수정, 취소 또는 복구한다.
- **기능 설명:**
  - 신청 항목: 이름, 전화번호, 주민등록번호 전체, 소속, 개인정보 수집·이용 동의
  - 신청 마감: 교육일 하루 전 17:00
  - 마감 이전: `정상 신청`
  - 마감 이후 및 교육 종료 후: `마감 후 신청`
  - 정원을 초과해도 신청 가능
  - 동일 전화번호가 존재하면 중복 경고 표시 후 신청 계속 허용
  - 신청자 목록: 이름, 전화번호, 주민등록번호 전체, 소속, 개인정보 동의 여부, 신청일시, 신청 구분, 신청 상태, 통화 상태, 문자 상태, 출석 상태, 만족도 응답 여부
  - 담당자 수정 가능: 이름, 전화번호, 주민등록번호, 소속
  - 신청 취소 시 삭제하지 않고 `취소` 상태로 유지
  - 취소 상태 복구 가능
  - 취소 신청자는 현재 신청 인원과 정원 초과 계산에서 제외
- **필요한 이유:** 신청 현황과 수강생 정보를 구글폼과 스프레드시트로 나누지 않고 교육별로 한곳에서 관리하기 위해 필요하다.

### 기능 3. 신청자 명단 다운로드
- **사용자 행동:** 담당자가 전체 또는 선택 신청자를 `.xls`로 내려받는다.
- **기능 설명:**
  - `전체 다운로드`, `선택 다운로드`
  - 다운로드 항목: 이름, 전화번호
  - 전화번호 하이픈 제거
  - 제외: 주민등록번호, 소속, 개인정보 동의 여부, 신청일시, 신청 구분, 신청 상태, 연락 상태, 출석 상태, 만족도 상태
  - 파일명: `YYYYMMDD_교육명_신청자명단.xls`
- **필요한 이유:** 전화·문자 등 외부 업무에 필요한 최소 정보만 바로 활용하기 위해 필요하다.

### 기능 4. 연락 및 출석 상태 관리
- **사용자 행동:** 담당자가 통화, 문자, 출석 상태를 변경한다.
- **기능 설명:**
  - 통화: `미통화 / 부재 / 통화완료`
  - 문자: `미발송 / 발송완료`
  - 출석: `미확인 / 출석 / 결석`
  - 통화와 문자는 독립적으로 변경
  - 취소 신청자는 출석 대상에서 제외
- **필요한 이유:** 연락 여부와 출석 여부를 별도 명단이나 기억에 의존하지 않고 신청자와 연결해 관리하기 위해 필요하다.

### 기능 5. 만족도 조사 운영 및 응답 현황 관리
- **사용자 행동:** 담당자가 QR 영역을 제공하고 수강생이 만족도 조사에 응답한다.
- **기능 설명:**
  - 문항 유형: 점수형, 주관식
  - 점수형: 1~5점
  - 응답자 식별: 이름
  - 동명이인 예외처리 없음
  - 최초 1회만 응답 가능
  - 응답 마감: 교육 종료일 기준 이틀 뒤 23:59
  - 이미 응답한 경우 중복 응답 안내
  - 응답 종료 후 신규 제출 차단
  - 담당자는 응답완료/미응답 인원 확인
  - 최초 버전에서는 Mock QR 이미지 사용
- **필요한 이유:** 교육마다 만족도 폼을 새로 만들지 않고 응답 현황을 한곳에서 관리하기 위해 필요하다.

### 기능 6. 만족도 결과 조회 및 공통 문항 관리
- **사용자 행동:** 담당자가 결과를 확인하고 공통 만족도 문항을 관리한다.
- **기능 설명:**
  - 전체 응답자 수
  - 문항별 평균 점수
  - 주관식 답변 모음
  - 사람별 상세 응답
  - 점수형/주관식 문항 추가
  - 문항 수정, 삭제
  - `위로 / 아래로`로 순서 변경
  - 변경된 공통 문항은 이후 새 교육부터 적용
  - 기존 교육 문항은 유지
- **필요한 이유:** 설문 결과를 별도 스프레드시트에서 취합하지 않고 교육별로 확인하며, 교육마다 설문을 다시 만드는 반복을 줄이기 위해 필요하다.

---

## 4. 화면 구성

### 화면 1. 관리자 로그인
- **화면명:** 관리자 로그인
- **화면 목적:** 관리자 영역 진입을 위한 최소 로그인 UI 제공
- **주요 구성 요소:** 서비스명, 아이디 또는 이메일, 비밀번호, `로그인` 버튼
- **사용자가 할 수 있는 행동:** 아이디/이메일과 비밀번호 입력, 로그인 버튼 클릭
- **다른 화면과의 이동 관계:** 최초 버전에서는 실제 인증 없이 로그인 클릭 시 `index.html`로 이동. 회원가입 화면은 없음.

### 화면 2. 교육 관리
- **화면명:** 교육 관리
- **화면 목적:** 전체 교육과 운영 현황을 확인하고 새 교육 생성 또는 상세 관리로 이동
- **주요 구성 요소:** 서비스명, `교육 관리`, `만족도 문항 설정`, `새 교육 생성`, 교육명 검색, 교육 목록, 교육명/일자/시간/장소/정원/신청/초과/출석/만족도 응답/교육 상태
- **사용자가 할 수 있는 행동:** 교육 목록 확인, 검색, 새 교육 생성, 교육 상세 이동, 만족도 문항 설정 이동
- **다른 화면과의 이동 관계:** `login.html` → `index.html`, `새 교육 생성` → `education-form.html`, 교육 선택 → `education-detail.html`, 만족도 문항 설정 → `survey-settings.html`

### 화면 3. 교육 생성 / 수정
- **화면명:** 교육 생성 / 수정
- **화면 목적:** 교육 기본 정보 입력 및 자동 신청 마감 확인
- **주요 구성 요소:** 교육명, 교육일, 시작시간, 종료시간, 장소, 정원, 자동 신청 마감, 공통 신청 항목 안내, 공통 만족도 문항 적용 안내, `교육 생성/저장`, `취소`
- **사용자가 할 수 있는 행동:** 교육 정보 입력, 자동 마감 확인, 생성, 수정, 취소
- **다른 화면과의 이동 관계:** 교육 관리 또는 교육 상세 → `education-form.html`, 저장 후 → `education-detail.html`

### 화면 4. 교육 상세 / 운영 관리
- **화면명:** 교육 상세 / 운영 관리
- **화면 목적:** 하나의 교육을 기준으로 신청자, 다운로드, 연락, 출석, 만족도 운영 및 결과를 관리
- **주요 구성 요소:**
  - 상단: 교육명, 교육일, 교육시간, 장소, 정원, 신청 인원, 초과 인원, 신청 마감, 교육 상태
  - 버튼: `교육 정보 수정`, `신청 페이지 보기`, `신청 링크 복사`
  - 탭 1 신청자 관리: 정상/마감 후/중복/취소 표시, 수정, 취소/복구, 전체/선택 다운로드
  - 탭 2 연락 관리: 통화 상태, 문자 상태, 요약 인원
  - 탭 3 출석 관리: 출석, 결석, 미확인
  - 탭 4 만족도 관리: Mock QR, 응답 마감, 응답완료/미응답, 응답기간 종료
  - 탭 5 만족도 결과: 응답자 수, 문항별 평균, 주관식, 사람별 상세
- **사용자가 할 수 있는 행동:** 신청자 수정/취소/복구, 명단 다운로드, 통화/문자/출석 상태 변경, 만족도 응답 현황 및 결과 확인
- **다른 화면과의 이동 관계:** `index.html` → `education-detail.html`, 수정 → `education-form.html`, 신청 페이지 → `apply.html`, Mock QR → `survey.html`

### 화면 5. 교육 신청
- **화면명:** 교육 신청
- **화면 목적:** 수강생이 회원가입 없이 교육 신청
- **주요 구성 요소:** 교육명, 일자, 시간, 장소, 신청 마감, 마감 후/종료 후 안내, 이름, 전화번호, 주민등록번호 전체, 소속, 개인정보 동의, 중복 경고, `교육 신청하기`, 완료 안내
- **사용자가 할 수 있는 행동:** 신청 정보 입력, 개인정보 동의, 중복 경고 확인, 계속 신청, 정상/마감 후 신청 완료 확인
- **다른 화면과의 이동 관계:** 교육 상세 또는 공유 링크 → `apply.html`

### 화면 6. 만족도 조사
- **화면명:** 만족도 조사
- **화면 목적:** 수강생이 교육 종료 후 만족도 조사에 최초 1회 응답
- **주요 구성 요소:** 교육명, 이름, 점수형 문항, 주관식 문항, `만족도 조사 제출`, 완료/중복/기간종료 안내
- **사용자가 할 수 있는 행동:** 이름 입력, 점수형/주관식 응답, 제출, 완료/중복/기간종료 확인
- **다른 화면과의 이동 관계:** 교육 상세의 Mock QR → `survey.html`

### 화면 7. 만족도 문항 설정
- **화면명:** 만족도 문항 설정
- **화면 목적:** 이후 생성되는 교육에 적용할 공통 만족도 문항 관리
- **주요 구성 요소:** 문항 목록, 유형, `문항 추가`, 수정, 삭제, `위로/아래로`, `변경사항 저장`, 이후 새 교육부터 적용 안내
- **사용자가 할 수 있는 행동:** 문항 추가, 수정, 삭제, 순서 변경, 저장
- **다른 화면과의 이동 관계:** `index.html` → `survey-settings.html`, 저장 후 → `index.html`

---

## 5. 디자인 방향

- **전체 분위기:** ______________________________
- **주요 색상:** ______________________________
- **참고하고 싶은 서비스:** ______________________________
- **모바일 대응 여부:** ______________________________

### 디자인 구현 원칙
- 정보 가독성과 상태 구분을 우선한다.
- 관리자 화면은 PC 사용을 우선한다.
- `정상 신청`, `마감 후 신청`, `중복 신청`, `취소`, `출석`, `결석`, `응답완료`, `미응답`은 텍스트 배지로 표시한다.
- 기본 HTML 폼, 테이블, 버튼, 탭 중심으로 구현한다.
- 복잡한 애니메이션과 CSS 프레임워크는 사용하지 않는다.

---

## 6. 개발 제약사항

- 관리자 로그인 UI를 구현한다.
- 최초 버전에서는 실제 로그인 인증을 연결하지 않는다.
- 로그인 버튼은 최초에는 관리자 화면으로 이동하는 동작만 구현한다.
- 실제 로그인/회원가입 기능이 필요해지면 추후 Supabase Auth를 연동한다.
- 회원가입은 최초 버전에서 구현하지 않는다.
- 데이터베이스는 최초 버전에서 연결하지 않는다.
- 실제 데이터 저장이 필요해지면 추후 Supabase Database를 연동한다.
- 필요한 데이터는 Mock Data를 사용한다.
- 실제 주민등록번호나 실제 개인정보는 사용하지 않는다.
- 불필요한 외부 API를 사용하지 않는다.
- QR 생성 외부 API를 사용하지 않는다.
- 실제 전화·문자 발송 기능을 구현하지 않는다.
- 명세에 없는 기능을 임의로 추가하지 않는다.
- 핵심 기능과 화면 구현을 우선한다.

---

## 7. 기술 스택 및 개발 규칙

- 기술 스택: HTML + CSS + JavaScript
- React, Vue 등 프레임워크를 사용하지 않는다.
- npm, Vite 등 별도의 빌드 도구를 사용하지 않는다.
- 별도의 서버 없이 정적 웹사이트로 구현한다.
- 화면 정의서에 정의된 주요 화면은 각각 하나의 HTML 파일로 구성한다.
- 공통 스타일은 CSS 파일로 분리한다.
- 공통 동작은 JavaScript 파일로 분리한다.
- 파일명과 폴더명은 영문 소문자로 작성한다.
- 필요한 샘플 데이터는 Mock Data를 사용한다.
- 로그인, 회원가입, 데이터베이스 기능이 필요해지면 추후 Supabase로 연계한다.
- 최초 버전에서는 Supabase SDK, URL, Key 등 실제 연결 설정을 추가하지 않는다.
- 최종 결과물은 Vercel에서 바로 배포할 수 있는 구조로 만든다.

### 권장 파일 구조

```text
/
├── login.html
├── index.html
├── education-form.html
├── education-detail.html
├── apply.html
├── survey.html
├── survey-settings.html
├── css/
│   └── style.css
├── js/
│   ├── app.js
│   ├── auth.js
│   └── mock-data.js
└── assets/
    └── images/
        └── survey-qr.png
```

### 로그인 구현 규칙
- `login.html`에 아이디 또는 이메일, 비밀번호 입력 필드를 제공한다.
- `로그인` 버튼을 제공한다.
- 최초 버전의 로그인 버튼은 실제 인증 없이 `index.html`로 이동한다.
- 입력값이 비어 있으면 간단한 안내 메시지를 표시할 수 있다.
- 회원가입 화면은 만들지 않는다.
- 추후 Supabase 연동이 가능하도록 로그인 관련 코드는 `auth.js`에 분리한다.

### 데이터 규칙
- 최초 버전에서는 DB를 연결하지 않는다.
- 필요한 화면 상태는 Mock Data를 사용한다.
- 데모 상태 유지가 필요한 경우 `localStorage`를 사용할 수 있다.
- `localStorage`는 프로토타입 용도로만 사용한다.
- 실제 개인정보는 저장하지 않는다.

### 교육 상태 규칙
- 신청 마감: 교육일 하루 전 17:00
- 신청 마감 전: `신청 접수 중`
- 신청 마감 후~교육 종료 전: `마감 후 접수`
- 교육 종료 후: `교육 종료`

### 신청 구분 규칙
- 신청 마감 이전: `정상 신청`
- 신청 마감 이후: `마감 후 신청`
- 교육 종료 이후: `마감 후 신청`

### 신청 인원 계산 규칙
- `취소` 상태는 현재 신청 인원에서 제외한다.
- 정원 초과 인원 = 현재 신청 인원 - 정원
- 계산값이 0 이하이면 초과 인원은 0으로 표시한다.

### 중복 신청 규칙
- 동일 교육에서 동일 전화번호가 존재하면 경고를 표시한다.
- 신청은 차단하지 않는다.
- 사용자가 계속 진행하면 별도 신청 건으로 유지한다.

### 신청자 수정 규칙
- 담당자만 수정 가능
- 수정 가능: 이름, 전화번호, 주민등록번호, 소속
- 수정 불가: 최초 신청일시, 신청 구분, 최초 개인정보 동의 여부

### 신청 취소 규칙
- 데이터를 삭제하지 않는다.
- `취소` 상태로 변경한다.
- 취소 상태는 복구 가능하다.
- 취소 상태는 현재 신청 인원과 출석 대상에서 제외한다.

### `.xls` 다운로드 규칙
- 외부 라이브러리를 사용하지 않는다.
- 브라우저에서 Excel이 열 수 있는 HTML 테이블 Blob 방식으로 구현한다.
- 다운로드 항목: 이름, 전화번호
- 전화번호 하이픈 제거
- 전체 다운로드와 선택 다운로드 제공
- 파일명: `YYYYMMDD_교육명_신청자명단.xls`

### 만족도 응답 규칙
- 이름으로 응답자를 구분한다.
- 동명이인 예외 처리는 하지 않는다.
- 최초 1회만 응답 가능하다.
- 응답 마감은 교육 종료일 기준 이틀 뒤 23:59이다.
- 이미 응답한 경우 중복 응답 안내를 표시한다.
- 기간 종료 후 신규 제출을 차단한다.

### 공통 만족도 문항 규칙
- 문항 유형: 점수형, 주관식
- 점수형은 1~5점
- 공통 문항 변경은 이후 새로 생성하는 교육부터 적용한다.
- 기존 교육의 문항은 유지한다.

### Supabase 연계 원칙
- 최초 버전에서는 Supabase를 연결하지 않는다.
- 추후 실제 로그인/회원가입이 필요하면 Supabase Auth를 연동한다.
- 추후 실제 데이터 저장이 필요하면 Supabase Database를 연동한다.
- 최초 버전에서는 Supabase SDK, 프로젝트 URL, API Key, 테이블 구조를 작성하지 않는다.

### 배포 규칙
- 별도 빌드 과정 없이 동작해야 한다.
- 정적 파일만으로 실행되어야 한다.
- 최초 진입 화면은 `login.html`로 한다.
- 최종 결과물은 Vercel 정적 배포 환경에서 바로 동작할 수 있어야 한다.

---

## 8. 완료 기준

### 관리자 로그인
- 사용자는 `login.html`에서 아이디 또는 이메일과 비밀번호를 입력할 수 있다.
- 사용자는 `로그인` 버튼을 누를 수 있다.
- 최초 버전에서는 실제 인증 없이 `index.html`로 이동한다.
- 회원가입 기능은 존재하지 않는다.
- Supabase 실제 연결은 존재하지 않는다.

### 교육 관리
- 사용자는 Mock 교육 목록을 확인할 수 있다.
- 교육명, 날짜, 시간, 장소, 정원, 신청 인원, 초과 인원, 출석 인원, 만족도 응답 인원을 확인할 수 있다.
- 교육 상태가 `신청 접수 중 / 마감 후 접수 / 교육 종료`로 표시된다.
- 교육명을 검색할 수 있다.
- 새 교육 생성 화면으로 이동할 수 있다.
- 교육 상세 화면으로 이동할 수 있다.
- 만족도 문항 설정 화면으로 이동할 수 있다.

### 교육 생성 / 수정
- 사용자는 교육명, 교육일, 시작시간, 종료시간, 장소, 정원을 입력할 수 있다.
- 교육일을 입력하면 하루 전 17:00이 신청 마감으로 표시된다.
- 교육 생성 또는 수정 동작을 확인할 수 있다.
- 저장 후 교육 상세 화면으로 이동할 수 있다.

### 교육 신청
- 수강생은 교육 정보와 신청 마감 시각을 확인할 수 있다.
- 이름, 전화번호, 주민등록번호, 소속을 입력할 수 있다.
- 개인정보 동의가 없으면 신청을 완료할 수 없다.
- 동일 전화번호가 존재하면 중복 경고를 확인할 수 있다.
- 중복이어도 계속 신청할 수 있다.
- 신청 마감 이전이면 `정상 신청`, 이후 또는 교육 종료 후이면 `마감 후 신청`으로 처리된다.

### 신청자 관리
- 담당자는 신청자 목록에서 Mock 개인정보와 신청일시, 신청 구분, 신청 상태를 확인할 수 있다.
- 정상 신청과 마감 후 신청이 같은 목록에 표시된다.
- 마감 후 신청자는 배지로 표시된다.
- 중복 신청자는 `중복 신청` 문구로 구분된다.
- 신청자 정보를 수정할 수 있다.
- 신청자를 `취소` 상태로 변경할 수 있다.
- 취소 상태를 다시 복구할 수 있다.
- 취소 신청자는 현재 신청 인원에서 제외된다.

### 신청자 명단 다운로드
- 전체 신청자를 `.xls`로 다운로드할 수 있다.
- 체크박스로 선택한 신청자만 `.xls`로 다운로드할 수 있다.
- 파일에는 이름과 전화번호만 포함된다.
- 전화번호에는 하이픈이 없다.
- 파일명은 `YYYYMMDD_교육명_신청자명단.xls` 형식이다.

### 연락 관리
- 통화 상태를 `미통화 / 부재 / 통화완료`로 변경할 수 있다.
- 문자 상태를 `미발송 / 발송완료`로 변경할 수 있다.
- 통화와 문자 상태는 독립적으로 변경된다.
- 미통화, 부재, 문자 미발송 인원을 확인할 수 있다.

### 출석 관리
- 출석 상태를 `미확인 / 출석 / 결석`으로 변경할 수 있다.
- 출석, 결석, 미확인 인원을 확인할 수 있다.
- 취소 신청자는 출석 대상에서 제외된다.

### 만족도 관리
- Mock QR 이미지를 확인할 수 있다.
- Mock QR을 클릭하면 만족도 조사 화면으로 이동한다.
- 응답 마감 시각을 확인할 수 있다.
- 응답완료 인원과 미응답 인원을 확인할 수 있다.
- 기간 종료 시 `응답기간 종료` 상태를 확인할 수 있다.

### 만족도 조사
- 수강생은 이름을 입력할 수 있다.
- 점수형 문항에 1~5점으로 응답할 수 있다.
- 주관식 답변을 입력할 수 있다.
- 최초 1회 응답 후 완료 메시지를 확인할 수 있다.
- 재응답 시 중복 응답 안내를 확인할 수 있다.
- 교육 종료일 기준 이틀 뒤 23:59 이후에는 제출할 수 없다.

### 만족도 결과
- 전체 응답자 수를 확인할 수 있다.
- 문항별 평균 점수를 확인할 수 있다.
- 주관식 답변 목록을 확인할 수 있다.
- 특정 응답자의 상세 응답을 확인할 수 있다.

### 만족도 문항 설정
- 현재 공통 만족도 문항을 확인할 수 있다.
- 점수형 문항을 추가할 수 있다.
- 주관식 문항을 추가할 수 있다.
- 기존 문항을 수정하거나 삭제할 수 있다.
- `위로 / 아래로` 버튼으로 순서를 변경할 수 있다.
- 변경사항을 저장할 수 있다.
- 변경된 문항은 이후 생성되는 교육에만 적용되는 동작을 Mock Data로 확인할 수 있다.

### 최종 배포
- `login.html`에서 관리자 진입 흐름을 확인할 수 있다.
- 모든 주요 화면이 각각의 HTML 파일로 열릴 수 있다.
- 화면 간 링크가 정상적으로 동작한다.
- 별도 빌드 명령 없이 브라우저에서 동작한다.
- 서버와 데이터베이스 없이 Mock Data 기반으로 주요 기능을 확인할 수 있다.
- 실제 로그인 인증이나 Supabase 연결 없이 최초 프로토타입이 동작한다.
- Vercel에 정적 파일을 배포하면 전체 흐름을 확인할 수 있다.
